---
phase: 02-sessions-upload
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - app/services/finanz_sse_service.py
  - app/routes/finanzberatung/sse.py
  - app/routes/finanzberatung/upload.py
  - templates/finanzberatung/session_detail.html
  - templates/hub/base.html
  - static/js/finanz_notifications.js
autonomous: true
requirements: [UPLD-06, SESS-02]

must_haves:
  truths:
    - "Consultant sees real-time toast notification when customer uploads a document"
    - "Toast notifications appear globally on ANY Hub page, not just session detail"
    - "Session detail page shows live upload feed with rich cards per document"
    - "SSE auto-reconnects with exponential backoff on connection drop"
    - "Missed events are fetched via REST on reconnect (no lost uploads)"
    - "Upload route publishes SSE event after successful file storage"
    - "Notification sound plays on new upload (with toggle-off option)"
  artifacts:
    - path: "app/services/finanz_sse_service.py"
      provides: "Redis Pub/Sub SSE manager with publish and stream methods"
      exports: ["SSEManager", "sse_manager"]
      min_lines: 40
    - path: "app/routes/finanzberatung/sse.py"
      provides: "SSE streaming endpoint for real-time notifications"
      min_lines: 30
    - path: "static/js/finanz_notifications.js"
      provides: "Client-side SSE connection, toast notifications, live feed, auto-reconnect"
      min_lines: 80
  key_links:
    - from: "app/routes/finanzberatung/upload.py"
      to: "app/services/finanz_sse_service.py"
      via: "Publish event after successful upload"
      pattern: "sse_manager\\.publish"
    - from: "app/routes/finanzberatung/sse.py"
      to: "app/services/finanz_sse_service.py"
      via: "Stream events from Redis subscription"
      pattern: "sse_manager\\.stream"
    - from: "static/js/finanz_notifications.js"
      to: "app/routes/finanzberatung/sse.py"
      via: "EventSource connection"
      pattern: "EventSource.*finanzberatung/sse"
    - from: "static/js/finanz_notifications.js"
      to: "app/routes/finanzberatung/sessions.py"
      via: "Fetch missed uploads on reconnect"
      pattern: "fetch.*documents\\?since="
    - from: "templates/hub/base.html"
      to: "static/js/finanz_notifications.js"
      via: "Global toast mode script inclusion guarded by FINANZ_ENABLED"
      pattern: "finanz_notifications\\.js|FinanzNotifications|sessionId.*null"
---

<objective>
Wire real-time SSE notifications so consultants see document uploads the moment they happen. This adds the Redis Pub/Sub SSE service, the streaming endpoint, the client-side notification system (toast + live feed + reconnect), and wires the upload route to publish events. Toast notifications work globally on ANY Hub page per locked user decision.

Purpose: Real-time feedback is critical for the consultant workflow -- they show their screen to the customer and need to see uploads appear immediately (UPLD-06). Also completes the session detail page experience.
Output: Working SSE pipeline from upload -> Redis -> streaming endpoint -> browser toast + live feed. Global toast on all Hub pages.
</objective>

<execution_context>
@C:/Users/Luke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sessions-upload/02-CONTEXT.md
@.planning/phases/02-sessions-upload/02-RESEARCH.md
@.planning/phases/02-sessions-upload/02-01-SUMMARY.md
@.planning/phases/02-sessions-upload/02-02-SUMMARY.md
@.planning/phases/02-sessions-upload/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 - Upload service (to add SSE publish call) -->
From app/services/finanz_upload_service.py:
```python
class FinanzUploadService:
    def store_file(file_storage, session_id, token_id, original_filename, mime_type) -> FinanzDocument
```

<!-- From Plan 02 - Sessions routes (documents endpoint for reconnect) -->
From app/routes/finanzberatung/sessions.py:
```python
# GET /sessions/<id>/documents?since=<timestamp> -- returns JSON array of documents
```

<!-- From Plan 02 - Session detail template (to add SSE script) -->
From templates/finanzberatung/session_detail.html:
```html
<!-- Has <div id="upload-feed"> container for live document cards -->
<!-- Has QR code card, notes editor, document list -->
```

<!-- Redis configuration from Phase 1 -->
From app/__init__.py (Celery config):
```python
# Redis DB 0: sessions, DB 1: Celery broker, DB 2: Celery results
# SSE should use DB 0 (general purpose) or a new DB 3
```

From app/core/extensions.py:
```python
# Redis client initialization with graceful degradation pattern
```

From app/config/base.py:
```python
class FinanzConfig:
    FINANZ_ENABLED = get_env_bool("FINANZ_ENABLED", False)
```

CONTEXT.md locked decisions for SSE:
- Dual notification system: toast notifications globally (any page) + live feed on session detail page
- Toast: slide-in bottom-right corner, 'Neues Dokument: filename von Kundenname', auto-dismiss 5s, click navigates
- Subtle notification sound with toggle-off option in settings
- Live feed: rich cards per upload showing file icon, filename, file size, timestamp, status badge
- Auto-reconnect with exponential backoff, visual indicator, missed events fetched on reconnect
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SSE service (Redis Pub/Sub) and streaming endpoint, wire upload route</name>
  <files>app/services/finanz_sse_service.py, app/routes/finanzberatung/sse.py, app/routes/finanzberatung/upload.py</files>
  <action>
1. Create `app/services/finanz_sse_service.py` with `SSEManager` class:
   - Use `logging.getLogger(__name__)` (NEVER print)
   - Initialize with Redis URL from `os.getenv('REDIS_URL', 'redis://localhost:6379/0')`
   - Use Redis DB 0 for pub/sub (same as sessions -- pub/sub is separate from key-value, no conflict)
   - Graceful degradation: if Redis unavailable, log warning and make publish a no-op

   Methods:
   - `__init__(self, redis_url=None)`:
     Connect to Redis with error handling. Set `self.redis = None` if connection fails.
   - `publish(self, session_id, event_type, data)`:
     Channel: `finanz:session:{session_id}`
     Message format: JSON `{event: event_type, data: data, timestamp: iso_now}`
     If redis is None, log warning and return (graceful degradation).
   - `stream(self, session_id)`:
     Generator that yields SSE-formatted events.
     Subscribe to channel `finanz:session:{session_id}`.
     Use `pubsub.get_message(timeout=30)` in a loop (not `pubsub.listen()` which blocks forever).
     On message: yield `f"event: {event_type}\ndata: {json_data}\nid: {timestamp}\n\n"`
     On timeout (no message): yield heartbeat `": heartbeat\n\n"`
     Include `retry: 5000\n` on first event (tells browser to reconnect after 5s)
     Clean up pubsub on GeneratorExit.
   - `format_upload_event(self, document)`:
     Helper to format a FinanzDocument into SSE event data dict:
     `{id, original_filename, file_size, mime_type, status, created_at, session_id, customer_name}`

   Create module-level singleton:
   ```python
   sse_manager = SSEManager()
   ```

2. Create `app/routes/finanzberatung/sse.py` with sub-blueprint `sse_bp = Blueprint('finanz_sse', __name__)`:
   - `@require_login` on all routes (only consultants see SSE stream)

   Routes:
   - `GET /sse/stream/<int:session_id>` -- SSE streaming endpoint
     - Verify user has access to this session (own session or admin)
     - Return `Response(stream_with_context(sse_manager.stream(session_id)))` with headers:
       - `Content-Type: text/event-stream`
       - `Cache-Control: no-cache`
       - `X-Accel-Buffering: no` (disables nginx buffering per RESEARCH.md pitfall 3)
       - `Connection: keep-alive`

   - `GET /sse/global` -- Global SSE stream for toast notifications
     - Subscribe to all sessions owned by current user (use pattern subscribe `finanz:session:*` and filter by ownership in the generator)
     - Alternative simpler approach: subscribe to user-specific channel `finanz:user:{username}` and have publish() also publish to the user channel
     - Choose the user-channel approach: cleaner, less Redis overhead

   Update `SSEManager.publish()` to also publish to `finanz:user:{opener_username}` channel (fetch opener from session). This enables global toast notifications on any page.

3. Modify `app/routes/finanzberatung/upload.py` -- Add SSE publish after successful upload:
   - Import: `from app.services.finanz_sse_service import sse_manager`
   - In the POST `/upload/<token>/submit` route, AFTER `upload_service.store_file()` succeeds:
     ```python
     # Publish SSE event for real-time notification
     event_data = sse_manager.format_upload_event(document)
     sse_manager.publish(token.session_id, 'new_upload', event_data)
     ```
   - This triggers both session-specific feed and user-specific toast
  </action>
  <verify>
    <automated>cd C:/Users/Luke/Documents/Slots/slot_booking_webapp && python -c "
from app.services.finanz_sse_service import SSEManager, sse_manager
from app.routes.finanzberatung.sse import sse_bp
print(f'SSEManager methods: {[m for m in dir(SSEManager) if not m.startswith(\"_\")]}')
print(f'SSE Blueprint: {sse_bp.name}')
print('SSE service and route imported successfully')
"</automated>
  </verify>
  <done>SSE service uses Redis Pub/Sub with graceful degradation. Streaming endpoint returns text/event-stream with heartbeats. Upload route publishes events after successful file storage. Both session-channel and user-channel publish for detail feed and global toast.</done>
</task>

<task type="auto">
  <name>Task 2: Create client-side notification system and wire globally into Hub base template</name>
  <files>static/js/finanz_notifications.js, templates/finanzberatung/session_detail.html, templates/hub/base.html</files>
  <action>
1. Create `static/js/finanz_notifications.js` -- Reusable notification module for Finanzberatung:

   The JS file must work in two contexts:
   - **Session detail page**: Shows live upload feed cards + toast notifications (sessionId set)
   - **Any Hub page**: Shows toast notifications only -- global stream (sessionId = null)

   **SSE Connection Manager:**
   ```javascript
   class FinanzNotifications {
     constructor(options) {
       this.sessionId = options.sessionId || null;  // null = global mode
       this.username = options.username;
       this.feedContainer = options.feedContainer || null;  // DOM element for live feed
       this.soundEnabled = localStorage.getItem('finanz_sound') !== 'false';
       this.evtSource = null;
       this.reconnectDelay = 1000;
       this.lastEventId = null;
     }
   ```

   **Methods:**
   - `connect()`:
     - If sessionId: connect to `/finanzberatung/sse/stream/{sessionId}`
     - If no sessionId (global mode): connect to `/finanzberatung/sse/global`
     - Set up event handlers: onmessage, onerror, custom events
   - `disconnect()`:
     - Close EventSource, clear reconnect timers
   - `handleEvent(event)`:
     - Parse JSON data
     - Show toast notification
     - If feedContainer exists, add document card to live feed
     - Play notification sound if enabled
     - Update lastEventId
   - `showToast(data)`:
     - Create toast element: slide-in from bottom-right
     - Content: "Neues Dokument: {filename} von {customer_name}" (per CONTEXT.md)
     - Auto-dismiss after 5 seconds (per CONTEXT.md)
     - Click navigates to session detail page
     - Style: DaisyUI `alert` with shadow, rounded, gold accent border
     - Appends to `#finanz-toast-container` element
   - `addToFeed(data)`:
     - Create rich card for the upload-feed container:
       - File icon: PDF icon (for application/pdf) or image icon (for image/*)
       - Original filename
       - File size (human-readable: KB/MB)
       - Upload timestamp (relative: "vor 2 Sekunden")
       - Status badge (DaisyUI badge-success for uploaded)
     - Prepend to feed container (newest first)
     - Animate entry with CSS transition (slide-in from top)
   - `playSound()`:
     - Create Audio element with a subtle notification sound
     - Use a short, professional chime (base64-encoded data URI for a simple tone, or a small .mp3 in static/)
     - Only play if `this.soundEnabled` is true
   - `toggleSound()`:
     - Toggle `this.soundEnabled`, persist to localStorage
     - Update UI toggle button state
   - `reconnect()`:
     - Exponential backoff: 1s, 2s, 4s, 8s, max 30s (per CONTEXT.md)
     - Show connection status indicator: "Verbindung unterbrochen..." / "Wieder verbunden" (per CONTEXT.md)
     - On reconnect: fetch missed events via REST:
       `GET /finanzberatung/sessions/{sessionId}/documents?since={lastEventTimestamp}`
     - Add missed documents to feed
     - Reset reconnectDelay on successful connection

   **Connection status indicator:**
   - Small dot/text in corner: green "Verbunden" / red "Verbindung unterbrochen..."
   - On reconnect attempt: yellow "Verbinde..."

2. Update `templates/finanzberatung/session_detail.html`:
   - Add `<script src="{{ url_for('static', filename='js/finanz_notifications.js') }}" nonce="{{ csp_nonce }}"></script>` before closing body
   - Add connection status indicator element near the upload feed section
   - Add sound toggle button (small speaker icon with on/off state) in the upload feed card header
   - Add `<div id="finanz-toast-container" class="toast toast-end toast-bottom z-50"></div>` inside the body
   - Initialize FinanzNotifications in inline script:
     ```html
     <script nonce="{{ csp_nonce }}">
       const notifications = new FinanzNotifications({
         sessionId: {{ session.id }},
         username: '{{ current_user }}',
         feedContainer: document.getElementById('upload-feed'),
       });
       notifications.connect();
       // Clean up on page unload
       window.addEventListener('beforeunload', () => notifications.disconnect());
     </script>
     ```
   - Style the upload feed container to show live cards with smooth animations
   - Ensure the document list section uses `id="upload-feed"` for the container

3. **CRITICAL -- Wire global toast notifications into `templates/hub/base.html`** (per locked user decision: "toast notifications globally (any page)"):
   - Add a conditional block guarded by `FinanzConfig.FINANZ_ENABLED` check. The template context already has access to config values. Use a Jinja2 guard:
     ```html
     {% if finanz_enabled %}
     <!-- Finanzberatung: Global upload toast notifications -->
     <div id="finanz-toast-container" class="toast toast-end toast-bottom z-50"></div>
     <script src="{{ url_for('static', filename='js/finanz_notifications.js') }}" nonce="{{ csp_nonce }}"></script>
     <script nonce="{{ csp_nonce }}">
       // Global mode: sessionId=null triggers /sse/global endpoint (user-channel)
       const finanzNotifications = new FinanzNotifications({
         sessionId: null,
         username: '{{ current_user }}',
         feedContainer: null,  // No feed container in global mode -- toast only
       });
       finanzNotifications.connect();
       window.addEventListener('beforeunload', () => finanzNotifications.disconnect());
     </script>
     {% endif %}
     ```
   - Place this block before the closing `</body>` tag in hub/base.html
   - Ensure `finanz_enabled` is available in the template context: add it to `register_template_context()` in `app/__init__.py` (or alternatively use `config.FINANZ_ENABLED` if the app config is already available in templates). Check which approach exists -- the app likely already exposes `config` object in templates. If not, the simplest approach is to set `finanz_enabled=FinanzConfig.FINANZ_ENABLED` in the template context processor in `app/__init__.py:register_template_context()`.
   - The session_detail.html will also have the toast container and script, but since it overrides the content block, the hub/base.html script runs as well -- use a guard in finanz_notifications.js to avoid double-connecting: if `window._finanzNotificationsActive` is already set, skip connect. Set this flag in connect().

   This ensures that a consultant on ANY Hub page (dashboard, slot booking, admin, etc.) receives toast notifications when their customers upload documents.
  </action>
  <verify>
    <automated>cd C:/Users/Luke/Documents/Slots/slot_booking_webapp && python -c "
import os
assert os.path.exists('static/js/finanz_notifications.js'), 'Missing finanz_notifications.js'
content = open('static/js/finanz_notifications.js').read()
assert 'EventSource' in content, 'Missing EventSource usage'
assert 'reconnect' in content.lower(), 'Missing reconnect logic'
assert 'toast' in content.lower() or 'Toast' in content, 'Missing toast notification'
assert '_finanzNotificationsActive' in content, 'Missing double-connect guard'
detail = open('templates/finanzberatung/session_detail.html').read()
assert 'finanz_notifications.js' in detail, 'session_detail not loading notifications JS'
assert 'upload-feed' in detail, 'session_detail missing upload-feed container'
base = open('templates/hub/base.html').read()
assert 'finanz_notifications.js' in base, 'hub/base.html not loading finanz notifications JS for global toast'
assert 'finanz-toast-container' in base, 'hub/base.html missing toast container div'
assert 'finanz_enabled' in base or 'FINANZ_ENABLED' in base, 'hub/base.html missing FINANZ_ENABLED guard'
print('Notification system: JS file exists with EventSource, reconnect, toast, double-connect guard')
print('Session detail template loads notification JS and has feed container')
print('Hub base template loads finanz notifications JS globally with FINANZ_ENABLED guard')
"</automated>
  </verify>
  <done>Client-side notification system with EventSource SSE connection, toast slide-in bottom-right with auto-dismiss 5s, live feed cards on session detail, exponential backoff reconnect with missed event fetch, notification sound with toggle, connection status indicator. Global toast wired into hub/base.html guarded by FINANZ_ENABLED -- consultants on ANY Hub page receive upload notifications.</done>
</task>

</tasks>

<verification>
1. SSE service publishes to Redis and streams events with heartbeats
2. Upload route triggers SSE event after successful file storage
3. Session detail page connects to SSE stream and shows live upload feed
4. Toast notifications appear bottom-right with "Neues Dokument: filename von Kundenname"
5. Toast notifications work on ANY Hub page (not just session detail) via hub/base.html inclusion
6. Auto-reconnect with exponential backoff (1s, 2s, 4s, 8s, max 30s)
7. Missed events fetched via REST on reconnect
8. Sound notification plays (with toggle-off)
9. Connection status indicator shows "Verbunden" / "Verbindung unterbrochen..."
10. Double-connect guard prevents duplicate SSE connections on session_detail (which inherits hub/base.html)
</verification>

<success_criteria>
- When a customer uploads a document via mobile page, consultant's session detail page shows new document card in the live feed within 1 second
- Toast notification appears on session detail page with filename and customer name
- Toast notification also appears when consultant is on ANY other Hub page (dashboard, slots, admin, etc.)
- If network drops, "Verbindung unterbrochen..." indicator shows, auto-reconnect fires with backoff
- On reconnect, any missed uploads appear in the feed (fetched via REST endpoint)
- Sound toggle persists across page refreshes via localStorage
- SSE endpoint returns proper headers (text/event-stream, no-cache, X-Accel-Buffering: no)
- hub/base.html only loads finanz_notifications.js when FINANZ_ENABLED is true
</success_criteria>

<output>
After completion, create `.planning/phases/02-sessions-upload/02-04-SUMMARY.md`
</output>
